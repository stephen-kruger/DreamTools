<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.madibasoft.dreamtools"
    android:versionCode="21"
    android:versionName="@string/app_version" >

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".DreamToolsActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="SettingsActivity"/>
        <activity android:name=".ServiceSelectorActivity"/>
        <activity android:name="AboutActivity"/>
        <activity android:name="SatfinderActivity"/>
        <activity android:name="VideoActivity"/>
        <activity android:name="ZapActivity" android:theme="@style/ZapTitleTheme"/>
        <activity android:name="MovieActivity" android:theme="@style/ZapTitleTheme"/>
        <activity android:name="ControlActivity"/>
        <activity android:name="AddTimerActivity"/>
        <activity android:name="MonitorActivity" android:label="@string/monitor"/>
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.VIBRATE" />
    </application>

	
</manifest>